android.useAndroidX=true
android.enableJetifier=true
# R8 + pdfbox need headroom; CI runners OOM on the default heap (GC thrash).
# Ceiling only — does not change build outputs.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
