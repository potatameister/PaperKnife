p0a cleanup: removed window.onerror alert from main.tsx, fixed .gitignore splash line, lock 1.0.6->1.0.9, added eslint.config.js + fixed lint flags (needs npm i -D eslint later), SignatureTool 3x catch+toast, 9 tools handleFile toast on open fail
p0 leaks: page.cleanup after renders, destroy pdf in generateThumbnail/getPdfMetaData, new destroyPdf helper, PdfPreview destroy on unmount + rAF scroll throttle, workspace 50MB cap + 24h TTL + db.close + clearAllWorkspaces, activity tx race fix + db.close, Nuke/autoWipe clear workspaces, font-display swap, lint flags fixed (106 any-warns ok). tsc clean, eslint 0 errors, vite build ok
p1 lazy: jszip dynamic import in Split/Compress/PdfToImage/ExtractImages, tesseract.js dynamic in PdfToText, dropped jszip+tesseract from manualChunks. vendor-utils 142K->45K, jszip 97K on-demand. initial ~1.52M->~1.41M. tsc clean, build ok. route-lazy deferred (needs APK smoke test)
issue6 history fix: activity stores result buffer (25MB cap, old rows show expired), deleteActivity added, all 16 tools pass buffer, AndroidHistoryView rows tappable with Open/Download/Share/Delete sheet, Layout drawer redownload uses buffer fallback. tsc clean, build ok
